# Custom PostgreSQL Configuration for Stateless Mind
# This file is explicitly mounted to ensure settings are available on startup.

# Connection Settings
listen_addresses = '*'

# Replication Settings
wal_level = replica
archive_mode = on
archive_command = 'cp %p /archive/%f'
max_wal_senders = 10
hot_standby = on